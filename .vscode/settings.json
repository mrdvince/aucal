{
    "python.linting.enabled": false,
    "python.pythonPath": "/anaconda3/envs/fastxla/bin/python",
    "workbench.editorAssociations": [
        {
            "settings": {
                "files.exclude": {
                    "**/.git": true,         // this is a default value
                    "**/.DS_Store": true,    // this is a default value
        
                    "**/data": true, // this excludes all folders 
                                            // named "node_modules" from 
                                            // the explore tree
        
                    // alternative version
                    "data": true    // this excludes the folder 
                                            // only from the root of
                                            // your workspace 
                }
            }
        }
    ]
}